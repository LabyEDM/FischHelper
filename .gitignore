# AutoHotkey
*.ahk.bak
*.ahk~

# Config files (local user configs - ignore these)
config.ini
roddata.json

# Local fish data (user's downloaded copy - ignore root level only)
# But keep data/fishdata.json in repo as template
/fishdata.json

# Temporary files
*.tmp

# Windows
Thumbs.db
Desktop.ini

